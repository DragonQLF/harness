[package]
name = "harness-agent-claude"
version = "0.1.0"
edition = "2021"

[dependencies]
harness-ports = { path = "../../ports" }
serde_json = "1"
tokio = { version = "1", features = ["io-util", "process", "sync"] }
tokio-util = { version = "0.7", features = ["rt"] }
