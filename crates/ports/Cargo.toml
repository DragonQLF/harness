[package]
name = "harness-ports"
version = "0.1.0"
edition = "2021"

[dependencies]
harness-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["sync"] }
tokio-util = { version = "0.7", features = ["rt"] }
