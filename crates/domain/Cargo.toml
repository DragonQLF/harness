[package]
name = "harness-domain"
version = "0.1.0"
edition = "2021"

[dependencies]
ts-rs = { version = '10', features = ['serde-compat'] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
# Tests replay real log lines to prove an older file still loads.
serde_json = "1"
