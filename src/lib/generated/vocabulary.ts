// Generated by crates/app/src/vocabulary.rs — do not edit.
//
// Ids come from serialising the Rust enums, so a value here is by
// construction one the backend parses. Run `pnpm codegen` after
// changing them.

export interface Choice {
  id: string;
  name: string;
  hint: string;
}

/** Board columns, left to right. The order is the workflow. */
export const STATUSES: Choice[] = [
  { id: "backlog", name: "Later", hint: "Parked, not queued" },
  { id: "ready", name: "Ready", hint: "Waiting for an agent to pick it up" },
  { id: "running", name: "Working", hint: "An agent is on it now" },
  { id: "review", name: "Review", hint: "Finished, waiting to be read" },
  { id: "done", name: "Done", hint: "Approved and merged" },
];

/** Where an agent does its work. */
export const WORKTREE_MODES: Choice[] = [
  { id: "per_card", name: "Per card", hint: "A fresh branch and checkout for every card" },
  { id: "shared", name: "Shared", hint: "One long-lived branch for the project" },
  { id: "none", name: "None", hint: "Reads the main checkout, never writes" },
];

/** Who reads the diff when a run finishes. */
export const REVIEWERS: Choice[] = [
  { id: "director", name: "Director", hint: "The Director reads the diff first and only sends you what passes." },
  { id: "human", name: "You", hint: "Every finished run lands in your review queue." },
  { id: "nobody", name: "Nobody", hint: "Finished runs go straight to Done." },
];

/** What the Claude login offers. */
export const MODELS: Choice[] = [
  { id: "opus", name: "Opus", hint: "Deepest reasoning, highest cost" },
  { id: "sonnet", name: "Sonnet", hint: "The everyday worker" },
  { id: "haiku", name: "Haiku", hint: "Fast and cheap, for lookups" },
];

/** Every reach an agent can hold. `allowed_tools` in the backend is what
 *  each one means to a run. */
export const ALL_PERMISSIONS: string[] = ["Read", "Search", "Edit", "Write", "Git", "Web", "Shell"];
