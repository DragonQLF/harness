# Dívida técnica — reescrita a 2026-08-28, revista a 2026-08-30 (#99, #105)

Este ficheiro é **reescrito a cada passagem** a partir do que as decisões dizem,
não acumulado. A versão acumulada dentro do DECISIONS chegou a contradizer-se
(dizia que compaction/ts-rs/e2e estavam por fazer depois de #50/#51/#56 os
registarem como feitos).

| Item | Estado real | Aponta para |
|---|---|---|
| Compaction sob pedido na UI | Automática no arranque feita; falta botão | #50 |
| ts-rs | Feito; exceções manuais documentadas em `types.ts` | #51 |
| Verificação end-to-end | Feita com modelo real: `e2e_sidecar` (Ready → Review) e `grants_e2e` (dois perfis, dois runs, cada um só com o que lhe foi concedido) | #56, #96 |
| Curador + árvore `areas/` | **Removido (#120).** A promoção existia, estava testada, e nada a chamava — nenhuma máquina tinha um `areas/`. As notas do `report_work` derivam-se agora do log no momento em que entram no prompt de um run (`memory::notes_from`), sem passagem, sem marca d'água e sem segunda cópia em disco. O passe de julgamento com modelo deixa de estar em dívida: esquecer passou a ser orçamento (mais recentes primeiro, com tecto, e o que ficou de fora é dito), que não precisa de modelo nenhum | #58–#60, #77, #105, #120 |
| Triador: ficheiros protegidos no risco | Por fazer (operador ainda não os nomeia) | #55 |
| Analista semanal | Hoje é sob pedido; agendador é infraestrutura nova | #55 |
| Modo Espelho aponta-se sozinho | Não é um interruptor: "Work on Relay itself" procura o repositório do Relay entre os projectos (pelo remote), depois o checkout de onde este binário foi compilado, e só então clona. Um projecto qualquer já não pode reclamar o papel | #65 |
| Hooks de telemetria estruturada | Parciais: expirações de aprovação registadas (#78); o resto não | #24–#31, #78 |
| Grafo de commits com curvas | Hoje é lista classificada, não as pistas do design | #18b |
| Sandbox / confinamento de shell | Adiado conscientemente; pathguard cobre caminhos estruturados, Bash fica na allowlist. O guardo do Bash passou a distinguir leitura de escrita por segmento (`ls`/`cat`/`find` fora da worktree passam; `>`, `$(…)`, `tee` e `find -exec` continuam guardados) — é heurística declarada, não fronteira | #2, #62, #81 |
| UI de resultados no chat | Feito — resultado funde com a chamada pelo id, aninhamento por pai (`parent_tool_use_id`), detalhe expansível (chat + sessions) | #70 |
| Responder aprovações pela conversa | Por fazer: ferramenta `answer_pending(approve\|reject, reason)` para o Director transportar a resposta do operador. Guardas: só o pedido mais recente e só no turno imediatamente seguinte; nunca para destrutivas (`delete_card`, `reject_card` exigem botão). **Pré-requisito da voz** (#69) — sem ecrã a fila é invisível, e encaixa no `AskUserQuestion` já intercetado | #70, #69 |
| #73/1 Pausa por orçamento | **Feito** — `Card.budget_paused` + `SetBudgetPause`; start recusa até o tecto cobrir o gasto | #73, #74 |
| #73/3 Painéis desactualizados | Defesa feita: sequência por evento; buraco → refresh imediato de snapshot/activity/projects para toda a UI. **A causa estrutural fechou-se com o #87**: agentes, projectos, conversas e turnos deixaram de ter duas fontes de verdade — há um dono, e guardar e anunciar acontecem no mesmo passo. **A auditoria à derivação do RightNow foi feita (#89) e a derivação estava correcta**; o rail em si já não existe no código. **O argumento em que ela assentava deixou de valer com o #99**: o valor do contexto era um objecto novo a cada render — não havia dependências para ficarem erradas — e passou a ser memoizado, porque era ele que punha toda a app a re-renderizar por token. A lista de dependências foi derivada mecanicamente de todos os campos do objecto, portanto nada deixou de ser recalculado; mas a partir daqui **um campo novo no `Store` que não entre nessa lista fica preso no valor antigo**, e isso é silencioso. Os dois defeitos encontrados na auditoria não eram derivação, eram **cache e relógio**, e estão corrigidos. O que continua por auditar se a divergência voltar é o `inbox`, que ficou de fora do #87 | #73, #87, #89 |
| #73/4 Custo e turnos ao vivo | Por fazer: stream só traz totais no `done`; emitir intercalar por turno para ver aproximação de tectos antes de bater neles | #73 |
| Timers vivos (restantes) | "há 2 min" em rótulos (Overlays 146, Chat 106/577, Misc 311, Projects 306) ainda só actualiza ao re-render — usar o mesmo padrão de tick quando tocares. O tick do RightNow existia mas estava no componente errado (no strip fechado, que não mostra tempo nenhum) e passou para o rail aberto, que é onde vive o `duration(...)` da sessão (#89) | checklist do operador, #89 |
| Estado de chat dentro do provider geral | O lote por quadro e o `useMemo` do #99 tratam o sintoma; a correcção estrutural é o chat e o feed de execução terem provider próprio, para que um token não faça o provider geral renderizar de todo. Rejeitada na véspera da 0.3.10 por mexer em quem consome e encaminha os eventos | #99 |
| Lista de dependências do valor do contexto | **Fechada no mesmo passo em que se abriu**: `pnpm run check:store` (`scripts/store-deps.mjs`) lê o `useMemo` do store pelo AST e falha se um campo do objecto não contribuir para as dependências. Está no `check`, no CLAUDE.md e no workflow de release. Só ficam de fora as funções escritas ali, que fecham sobre setters do `useState` | #99 |
| `cargo test -p <crate>` corrompe os tipos gerados | O `export_types` corre por crate, e correr só um escreve `bigint` onde a árvore inteira escreve `number` (o #05: um u64 chega à janela como number). Quatro ficheiros mudaram assim numa sessão, e só o `tsc` deu por isso. Correr sempre `pnpm codegen` (workspace inteiro) depois de qualquer `cargo test -p` | #05 |
| Drag & drop, timer de inatividade, inspector do event log | Nunca começados | — |
| Custo de contexto dentro de uma execução | **Onde o dinheiro está, e por fechar.** As leituras iniciais não são o grosso: nas execuções medidas são 9–71, contra 19–104 de Bash e 15–120 de edições. O que se paga é o contexto relido a cada turno — mediana de 147k–200k tokens, contexto final de 215k e 303k, vezes 72 a 235 turnos. A memória do #120 trata a abertura de um run, não o corpo dele. Fechar é compactar dentro da execução e podar a saída de ferramentas da transcrição em vez de arrastar 200k por 150 turnos | #120 |
| Execuções fora da Anthropic não reportam tokens nenhuns | Os 362 eventos de `usage` da execução `b3f8b94c` (OpenRouter) vêm todos a zero. A #62 tirou o preço falso; não pôs um número verdadeiro no lugar, portanto essas execuções são invisíveis em qualquer vista de consumo. O adaptador tem os números do lado dele — falta trazê-los | #79 |
| Pré-conceder permissões antes da sessão | As permissões permanentes funcionam (145 dos 172 pedidos daquela conversa foram respondidos por elas em menos de 250ms, sem incomodar ninguém), mas só se cunham a partir de uma folha ao vivo: o `Chat.tsx` escreve-as e o `Settings.tsx` só as lista e remove. Ou seja, a funcionalidade que deixa o trabalho seguir sem o operador só se liga com o operador ali sentado a vê-la perguntar. Fecha-se com o caminho de escrita nas Settings — e com o backend a devolver a regra, em vez de o ecrã a prever replicando o `allow_always` | #105 |
| Fecho do dia bloqueia o fecho da janela | Narrado e escapável: overlay diz o que se espera, conta o tempo, e "Close now" corta (`closing.rs`). Tecto duro de 180s solta a janela aconteça o que acontecer | #79 |
| `self_report` relê transcrições inteiras por chamada | OK com semanas de dados; se crescer, filtrar por `ts_ms` ao ler em vez de depois | #78 |
| Caixa de entrada sem notificação fora do rail | **Propostas**: continua tal e qual — só aparecem no RightNow, nenhum badge na nav e nenhum toast quando chegam. **Trabalho fora do quadro**: já não é o mesmo caso (#88) — tem secção própria no rail, conta para o número do strip fechado e leva um toast à chegada, que é o que o alcança nos dois ecrãs onde o rail está escondido de propósito (`RAIL_HIDDEN = board, agents`). Nesses dois ecrãs o toast é o **único** sinal, e um toast passa: fechar isso é badge na nav ou banner, exactamente o que falta às propostas | #79, #86, #88 |
| Publicar uma versão exige um tag à mão | `git tag vX.Y.Z` dispara o workflow; o rascunho é publicado à mão de propósito. O número da versão vive em três ficheiros que têm de concordar (`tauri.conf.json`, `src-tauri/Cargo.toml`, `package.json`) e nada verifica que concordam | #79 |
| Cores continuam só no frontend | `TONE` e `STATUS_TONE` ficam em `types.ts` de propósito — o Rust não tem que saber o que um tom resolve. Desde #80 um tom é um conjunto de classes do Tailwind e já não uma variável CSS. É a única parte do vocabulário que não vem do backend | #51, #80 |
| Codegen de um crate só corrompe os tipos | `cargo test -p <crate> --test export_types` regenera tudo com `bigint` em vez de `number`: a feature do ts-rs só unifica ao construir o workspace inteiro. Usar sempre `pnpm codegen`; nada impede o contrário | #51 |
| Endpoints alternativos por testar com um modelo real | Ollama e OpenRouter estão ligados (três variáveis de ambiente por run) e o caminho compila, mas nenhum agente correu ainda contra um modelo que não seja da Anthropic. Falta saber como se portam as chamadas de ferramentas em modelos pequenos | #79 |
| Levantar um projecto frio pode construir dois engines | O `runtime()` pergunta, constrói fora e põe; entre o perguntar e o pôr cabem duas chamadas ao mesmo projecto, e cada uma abre o seu `JsonlStore` sobre o mesmo ficheiro de eventos. Já existia antes do #87 e ficou **estreitado**, não fechado: o mapa passou a ser o árbitro (`entry().or_insert()`), portanto só um dos dois é usado e o perdedor morre com o handle. Fechar a sério é *single flight* — o dono registar que alguém já está a construir e o segundo esperar por esse. Muda comportamento, logo ficou fora de uma passagem de estrutura | #87 |
| `settings` e `inbox` continuam atrás de um lock | Não é descuido (#87): os dois são lidos do guardo síncrono do fecho da janela em `lib.rs`, que tem de decidir o `prevent_close` antes de retornar, e o `settings` também do `AgentPort::run`, que é um método de trait sem `async` por decisão (#3). Um actor obrigaria a uma cópia síncrona ao lado — a segunda fonte de verdade que a migração veio remover. Se algum dia se fizer, é a assinatura do porto e o guardo do fecho que têm de mudar primeiro | #87, #3 |
| Testes do engine a falhar aos 30s do `wait_for` | **Resolvido — e a causa registada estava errada duas vezes.** Foram seis ocorrências; as quatro últimas todas no mesmo teste (`a_failed_run_leaves_work_and_the_next_run_finds_it`). Não era a carga (a quarta deu-se com a máquina só para ela) e não eram "duas worker threads bloqueadas", que é o que o `f56eeb3` concluiu. A prova está no próprio `wait_for`: ele só entra em `panic` **depois** de `check().await` voltar — com o actor preso o teste pendurava para sempre em vez de estourar aos 30,0s. Instrumentado, o actor respondeu a 93 sondagens em 2s enquanto "não progredia". O que havia era o teste a esperar por `active_runs()` não vazio, um estado **transitório**: o `WritesThenFailsAgent` morre no primeiro turno, portanto com poucas workers o segundo run nascia e acabava antes da primeira sondagem, e a janela nunca mais voltava a abrir. Passou a esperar pelo `RunStarted` desse run no log — um facto que só acumula. 50 corridas da suite com o reprodutor armado e duas a competir pela máquina: zero falhas, onde antes falhava à primeira ou à segunda | #97 |
| Duas falhas antigas do engine continuam por explicar | `a_shared_worktree_is_adopted_after_a_restart_not_rebuilt` e `the_loser_of_the_agent_limit_never_builds` falharam uma vez cada e **não** são a mesma forma que a de cima: ambos esperam por estados que colam (`Review` num; no outro um `active_runs` com um agente que só morre cancelado), portanto a correcção do #97 não os cobre. Ficaram sem reprodutor e sem mensagem guardada. Se voltarem, é por aí que se começa | #97 |
| `build_done` estacionava o artefacto dentro do loop do actor | **Resolvido (#97)**: o `create_dir_all`, o `copy` do binário construído e o `write` do manifesto corriam em `std::fs` cru no actor. O binário do orquestrador tem dezenas de MB e a worker thread do runtime ficava presa a copiá-lo — a doença do #46, no caminho do build em vez do git. Passou a `spawn_blocking`. O actor continua a esperar, e isso é de propósito (o manifesto tem de existir antes de o cartão ser anunciado, ou quem o lê a seguir lê um buraco); o que deixou de parar com ele é o runtime | #46, #97 |
| Concessões não chegam aos runs de cartão | As skills e os servidores MCP penduram-se no **porto** do agente, e uma conversa constrói o seu porto por perfil. O engine tem um `Arc<dyn AgentPort>` partilhado por todos os runs de cartões, portanto um worker não os leva. Fechar é acrescentar dois campos ao `RunSpec` (`skills_dir`, `mcp_servers`) e copiá-los do `RunProfile` em **dois literais** de `crates/engine`: `runs.rs:368` e `director.rs:74`. Ficou por fazer porque `crates/engine/` estava fechado a esta passagem, não porque falte desenho — o `crates/app/src/grants.rs::for_profile` já devolve exactamente o que esses dois campos levariam | #93 |
| As ferramentas de um MCP são declaradas, não confirmadas | O operador aprova a lista que o modelo leu na documentação. O Relay só sabe a verdadeira depois de se ligar ao servidor, e ligar-se é executar o código que a aprovação existe para travar. Falta o passo depois: o `mcpServerStatus()` do SDK devolve as ferramentas reais quando liga, portanto um run pode comparar o declarado com o servido e dizer no transcript quando não batem. Sem isso, uma declaração incompleta é uma aprovação falsa e ninguém dá por ela | #94 |
| Dezassete skills da CLI em todos os runs, medidas | `getContextUsage()`: 17 skills `built-in` e 2631 tokens em cada run, concedido ou não — `run`, `code-review`, `simplify`, `dataviz`, `init`, … Não é fuga (o `settingSources: []` continua a tirar as do `~/.claude` do operador) nem foi introduzido pelas concessões: com o plugin e `skills: 'all'` são 18 e 2650, e a que entra é a nossa. Desligá-las é `skills: []`, uma linha — mas é mudança de comportamento que ninguém pediu e não se sabe se alguma está a ser útil a um worker. Fica medido para o operador decidir | #96 |
| A CLI não carrega skills concedidas | O adapter `model-claude` (`claude --print`) não tem equivalente da opção `plugins` do SDK, e inventar um significaria escrever no `~/.claude` do operador — precisamente o que este desenho evita. Um agente com skills concedidas corre sem elas se o operador desligar o sidecar nas Settings, e nada lho diz | #93, #13 |
| Um servidor MCP sem chave falha ao ligar e ninguém conta | O `add_mcp_server` guarda **nomes** de variáveis de ambiente e nunca valores (a conversa é escrita para disco), e o ecrã dos agentes tem os campos. O que falta é o run dizer que não ligou por falta de chave em vez de a ferramenta simplesmente não existir — hoje o modelo vê a ausência, não a razão | #94 |
| O `permissions` do perfil aceita o que não está no vocabulário | `AgentProfile::allowed_tools` mapeia os sete nomes conhecidos e deixa passar **qualquer outro tal e qual** (`other => tools.push(other)`). O `ALL_PERMISSIONS` é a vocabulário declarada e o `grant_agent_tools` valida contra ela, mas um `agents.json` editado à mão põe o que quiser no `allowed_tools` de um run. Encontrado ao ler o caminho das concessões; não corrigido porque recusar entradas desconhecidas é mudança de comportamento num ficheiro que já existe em disco | #93 |
| Versão nunca sobe sozinha | Em 0.2.0 desde 2026-08-26; sobe à mão. O updater compara versões, logo um release novo com a mesma versão não é oferecido a ninguém | #79 |
| Updates só existem nesta máquina (histórico) | O Modo Espelho compila e estaciona o binário em `appdata/updates`: não há versão, não há canal, não há outro SO. Um Mac nunca vê o que este Windows construiu, e não há forma de dizer "esta é mais nova que a que tens". **Resolvido**: `tauri-plugin-updater` lê o `latest.json` do release mais recente, escolhe o asset do SO e verifica a assinatura antes de executar o que descarregou. A chave privada vive fora do repositório (`~/.relay`) e no secret `TAURI_SIGNING_PRIVATE_KEY` | #79 |
| Banner de update: o caminho do espelho ainda só lê ao montar | O feed de releases é consultado ao arrancar e de 3 em 3 horas, mas `updates_list` (builds parqueados por um cartão) continua a ler uma vez. O erro do install já é um toast | #79 |
| Migração para Tailwind por validar a olho | Feita e verificada por construção: `tsc --noEmit` limpo, `pnpm build` a construir, zero `style={{}}` estáticos, e a troca de tema confirmada num probe com as classes reais da app (escuro↔claro e o acento escolhido pelo operador a sobrepor-se). O que **não** foi feito é abrir cada um dos onze ecrãs no Tauri e compará-los com o antes: a app não arranca num browser (`getCurrentWindow()` precisa do runtime) e não há dev server configurado para ela | #80 |
| `.tile:hover` não tem par para o tema claro | Preservado tal e qual: os três valores crus do desenho (`#1e1d19`, `#33302b`) aplicam-se nos dois temas, portanto um cartão claro escurece ao passar por cima. Anotado, não corrigido — é do brief da v2 | #80 |
| `PRODUCT.md` diz que o acento é `#8b7cff`/`#5b53d8` | O `theme.css` tinha `#38adee`/`#0d74b8` e é isso que a migração levou para o `tailwind.config.js`. O documento está desactualizado desde alguma passagem anterior; a reconciliação do acento com o ícone novo é do brief da v2 e não foi tocada | #80 |
| Trabalho fora do quadro: só o Director o recebe | **Resolvido pelo lado do ecrã (#88)**: o `mirror://outside-work` tem escutador, secção "Outside the board" no RightNow, contagem no strip, toast à chegada e histórico do que chegou nesta janela. Não expira e só sai com o botão | #86, #88 |
| Trabalho fora do quadro: o aviso atravessa como prosa | **Resolvido (#90)**: o evento e o `bootstrap` levam um `MirrorWarning { work, for_director }` — o `OutsideWork` derivado em `TS` ao lado da metade que fala ao Director. O rail mostra chips (`3 commits · 12 files · oldest 4h ago`) e a lista de ficheiros como dados, e o `outsideWorkParts` — o corte de prosa numa marca literal — desapareceu: o corte vive agora onde a redacção vive (`mirror::FOR_DIRECTOR`), com um teste a garantir que a frase acaba exactamente aí. O parágrafo inteiro não atravessa de propósito: é `factos + FOR_DIRECTOR` e o ecrã nunca o mostrou inteiro | #86, #88, #90 |
| Trabalho fora do quadro: o aviso do arranque pode não ter ouvinte | **Resolvido (#91)**: campo `outside_work` no `Bootstrap`, lido do mesmo sítio que o evento emite, e o `look_for_outside_work` passou a guardar antes de anunciar. Arranque e recarregar recuperam-no. O duplicado morre por uma chave que são os factos (`commits · since_ms · files_total · files`) e não a frase, num `ref` e não na lista — a decisão tem de ser tomada na chamada, que é onde se sabe se o toast é devido | #86, #88, #91 |
| Trabalho fora do quadro: só o repositório do espelho | A verificação corre sobre o projecto do Modo Espelho e mais nenhum. Um projecto do operador em que ele commita à mão tem exactamente o mesmo problema e ninguém dá por ele. Foi âmbito, não descuido | #86 |
| Máquina de estados do quadro duplicada em TypeScript | **Resolvido (#92)**: o `Board.tsx` tinha o `Status::LEGAL_MOVES` copiado à mão, e não era decoração — o `drop()` recusava em silêncio e o `canDrop` acendia colunas a partir dele. O `vocabulary.rs` passa a escrever `LEGAL_MOVES` derivado de `Status::can_move_to`, pelo mesmo mecanismo dos ids | #92, #51 |
| Começar um cartão: o ecrã sabe um terço da regra | O `App.tsx` oferece "Start:" a qualquer cartão em `ready`; o motor exige também `!budget_paused` e todas as `depends_on` em `Done`. O `Card` traz os dois campos e o frontend não os lê, portanto a paleta oferece acções que o motor recusa com um toast. Escrever a regra em TS seria replicá-la — fecha-se com o backend a dizer se um cartão arranca e porquê | #92 |
| Um veredicto reconhecido pela prosa da etiqueta | **Resolvido (#105).** A `ActivityRow` ganhou o campo `approved` e o Home passou a usar só o `stats.done_today`, mas ficou um leitor por trás: o `Review.tsx` ainda pintava o ponto do histórico com `label.startsWith("Approved")`. O doc do próprio campo explicava porque é que isso está errado. Era o último | #89, #92, #105 |
| A lista de shells estava escrita à mão | **Resolvido (#105), e o culpado não era quem esta linha dizia.** O `ruleIsRevoked` do `types.ts` sempre consumiu o `SHELL_TOOLS` gerado, com um comentário a dizer porquê. Quem a escrevia à mão era o `Chat.tsx`, **duas vezes** — no `scopeOf` e no `scopable`, que é onde se decide o que uma permissão permanente cobre. Três cópias de uma regra de segurança, duas sem nada que as mantivesse a par. Continua por fazer a outra metade: o ecrã só sabe *prever* a regra que o backend vai gravar, e prevê-la replicando o `allow_always`. Fecha-se com o backend a devolver a regra antes de o operador clicar | #92, #105 |
| Tecto de orçamento inventado no `NavRail` | `settings?.daily_budget_usd ?? 10` — o `10` não existe em lado nenhum. Hoje é **inalcançável** (o rail só desenha depois de `ready`, e `ready` só sobe depois de o bootstrap pôr o `settings`), logo não é divergência a correr: é defesa morta à espera de passar a ser alcançável. Anotado e não corrigido, que seria mexer sem nada mudar | #92 |
| O `RunUpdate` à mão está atrás do `RunEvent` | **Resolvido (#105).** Eram dez campos (`count`, `tool_use_id`, `parent_tool_use_id`, `ok`, `detail`, `error` e os quatro contadores de tokens) lidos por *cast* em doze sítios — o compilador a ser dito para não olhar, num ficheiro cuja promessa é que se pode olhar. Estão declarados no `RunUpdate` e no `RunLogLine`, e os doze *casts* desapareceram por consequência. O que continua frouxo de propósito é a **união**: o `#[serde(flatten)]` sobre um enum com tag interna não tem forma que o ts-rs saiba escrever | #51, #92, #105 |
| O ecrã de detalhe de um projecto perdeu a estrada | **Alta.** O `ProjectPage` (`src/views/Projects.tsx:53`) são 474 linhas que nada importa desde o `6bc7309`, e não é código morto: é o **único** sítio da app que mostra o detalhe de um projecto — o grafo de commits, os ramos, as linguagens e os *checks* — e o único consumidor de quatro comandos vivos (`project_detail`, `project_checks`, `project_set_checks`, `project_run_checks`). O README diz que o ecrã de Código mostra "branches, languages and checks"; ele não mostra checks nenhuns, e ninguém mostra, portanto **configurar os checks de um projecto não tem interface**. Fechar é uma decisão de produto (um destino novo na nav, ou o conteúdo a mudar-se para o ecrã de Código), e por isso ficou fora de uma passagem de estrutura | #105 |
| Comandos registados sem porta no ecrã | **Deixou de ser uma linha e passou a ser um teste (#128).** O `pnpm run check:commands` lê o `generate_handler!` e o `ipc.ts` e falha em três casos: um comando registado sem embrulho, um embrulho para um comando que não existe (que só se descobria a correr), e um embrulho que ninguém chama. As ausências conhecidas vivem em duas listas no próprio script, cada uma com a razão escrita, e o guarda falha também ao contrário — uma excepção que deixou de o ser fica a mentir e é apanhada. Acrescentou três que ninguém tinha notado (`checks`, `codexStatus`, `inbox`) e uma que é pior do que estar sem porta: o `prepare_shutdown`, registado, invocado por ninguém, e a duplicar os dois passos que o `closing.rs` já faz | #105, #128 |
| Um perfil nasce em dois sítios com valores diferentes | O `custom()` do `Agents.tsx` constrói um `AgentProfile` inteiro à mão e cinco valores discordam do `AgentProfile::default()` do Rust (`model`, `budget_usd`, `worktree`, `provider`, `reviewer`). **É de propósito** e a razão está escrita lá: o que o Director cria vai trabalhar, o que sai daquele botão é um rascunho. O `tsc` garante a *forma* — o literal tem de satisfazer `AgentProfile`, portanto um campo novo no Rust parte ali — mas nada garante os valores. Deixado a duplicar, com a razão à vista | #105 |
| `catalog::is_local` decide por prefixo | `https://localhost.qualquer-coisa` passa por local, portanto leva um GET de `/api/tags`. Quem escreveu o endpoint foi o operador e o que se lhe manda não é segredo nenhum, mas a regra é mais larga do que a frase que a descreve. Prendido num teste com a verdade escrita, não apertado: recusar isto mudava endpoints que hoje funcionam | #105 |
| `truncate` são duas coisas com um nome | Uma função em `lib/format.ts` (corta texto) e uma classe do Tailwind em `components/ui.tsx` (`min-w-0 truncate`). Dez ficheiros importam a classe, dois a função, nenhum as duas — e o `tsc` apanha quem se enganar, porque as assinaturas não coincidem. Anotado e não corrigido: renomear atravessa dez ficheiros para fechar um risco que o compilador já fecha | #105 |
| Vocabulário do desenho sem consumidor | Onze componentes do `ui.tsx` (`CardHead`, `Avatar`, `Pill`, `StrongButton`, `PageHead`, `Icon`, …) e três variantes do `motion.ts` (`paneInDelayed`, `drawerUp`, `railIn`) não são importados por ninguém. Não se apagaram: é vocabulário declarado de um desenho, e o `railIn` até nomeia um rail que o #89 diz já não existir. Apagar tokens de desenho não é o que uma passagem de estrutura decide | #80, #89, #105 |
| `sidecar/mcp-wiring.test.mjs` não corria em lado nenhum | **Resolvido.** O `test:sidecar` nomeava só `pathguard` e `toolsum`; o teste de fiação existia e ninguém o corria — precisamente a classe de bug que ele foi escrito para apanhar, e apanhou-se a si próprio (o duplo do `z.enum` não tinha `.optional`, e uma ferramenta nova partiu-o em silêncio). Corrigido o duplo e acrescentado ao script | — |
